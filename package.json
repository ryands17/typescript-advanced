{
  "name": "typescript-advanced-concepts",
  "version": "1.0.0",
  "description": "Some Typescript examples",
  "main": "src/index.ts",
  "scripts": {
    "dev": "tsnd --respawn --transpile-only --no-notify ./src/index.ts",
    "clean": "rimraf dist",
    "build": "npm run clean && tsc"
  },
  "keywords": [],
  "author": "Ryan Dsouza",
  "license": "ISC",
  "dependencies": {
    "typescript": "^4.9.5"
  },
  "devDependencies": {
    "@types/node": "^18.14.6",
    "husky": ">=8.0.3",
    "lint-staged": ">=13.1.2",
    "prettier": "^2.8.4",
    "rimraf": "^4.3.1",
    "ts-node-dev": "^2.0.0"
  },
  "prettier": {
    "singleQuote": true
  },
  "lint-staged": {
    "*.{ts,json}": "prettier --write"
  }
}
